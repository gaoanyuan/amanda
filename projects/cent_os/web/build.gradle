apply plugin: 'jetty'
httpPort = 8080
jettyRun {
    reload ="automatic"
    scanIntervalSeconds = 1
}
dependencies {
    //compile project(':shared')
    compile(
            libraries.'spring-webmvc'
    )
}
